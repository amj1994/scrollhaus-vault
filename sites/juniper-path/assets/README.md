# Video assets not committed here

Two files are intentionally excluded from this folder because of their size:

- `hero-bg.mp4` (~11.6MB) — the hero background, pale-mint glass ribbons
- `footer-bg.mp4` (~5MB) — the footer/integrative-band background, ink-landscape with cranes

Upload both to your own video host (Gumlet, etc.) and update the `src` on
`#heroVideo` and `#footerVideo` in `../index.html` to point at the hosted URLs.
